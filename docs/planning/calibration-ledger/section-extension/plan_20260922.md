# Implementation Plan — aspect `section-extension` (2026-09-22)

Source: `docs/planning/calibration-ledger/prd.md` + `section-extension/spec.md`.
Read first: `src/belay/verify/triage_surfaces.py:195-263` (`skipped_verdict`,
`triage_section`), `src/belay/cli.py:1456-1488` (the text line),
`tests/test_verify_triage_surfaces.py:281-316` (the budgeted-mode pins),
`tests/test_refutation_triage.py:174-226` (the identity pin — must pass unmodified).

## 0. Setup
Worktree `.claude/worktrees/feat-calibration-ledger`, branch
`feat/calibration-ledger/aliz`. Confirm `uv run python -c "import belay;
print(belay.__file__)"` resolves inside the worktree.

## 1. Files
| File | Purpose |
|---|---|
| `src/belay/verify/triage_surfaces.py` (edit) | `triage_section`: skipped turns' score rows with `"skipped": true` in budgeted mode |
| `src/belay/cli.py` (edit) | `_triage_line`: `scored` counts all scored turns (incl. skipped); `skipped` unchanged |
| `tests/test_verify_triage_surfaces.py` (edit) | Re-pin the budgeted-mode section tests |

## 2. Phases (TDD, commit per phase)

### Phase 1 — RED
Extend the budgeted-mode tests in `tests/test_verify_triage_surfaces.py`: with scores
`{0: 0.9, 1: 0.2, 2: 0.6}` and threshold 0.5, the section's `scores` list now carries
three rows — ordinals 0 and 2 unchanged (no marker), ordinal 1 with `"skipped": true`.
Assert the shadow-mode section shape is UNCHANGED (the existing tests, untouched, stay
green — that is part of the RED discipline: only the budgeted-mode tests go red).
Also assert the text line: `scored` = 3 (was 2), `skipped` = 1.
Run: `uv run pytest tests/test_verify_triage_surfaces.py -q` — exactly the budgeted-mode
assertions fail. Commit: `git add tests/test_verify_triage_surfaces.py && git commit -m
"test(section-extension): RED — budgeted mode carries skipped turns' scores"`

### Phase 2 — GREEN
- `triage_section`: one row per scored turn in scope (non-None), with `"skipped": true`
  iff `n not in replay_indices`. Shadow mode: no skipped turns exist, so no row gains a
  marker — **the shadow section is byte-identical** (the `skipped: 0` section).
- `_triage_line`: `scored` counts all rows; `skipped` unchanged.
Run: `uv run pytest tests/test_verify_triage_surfaces.py tests/test_refutation_triage.py
-q` — all green INCLUDING the refutation identity test **unmodified** (that is the
acceptance). Commit: `git add src/belay/verify/triage_surfaces.py src/belay/cli.py &&
git commit -m "feat(section-extension): skipped turns' scores in the budgeted triage
section — shadow byte-unchanged"`

### Phase 3 — suite
`uv run pytest tests/test_verify_triage_surfaces.py tests/test_refutation_triage.py
tests/test_verify_triage.py tests/test_cli_flag_parity.py tests/test_verify_json.py -q`
(the JSON snapshot pin must stay green — the section change must not alter the snapshot
fixture). Full suite only if practical.

## 3. Agent notes
Checkpoint after Phase 2. Do NOT touch `triage.py`, `triage_budget.py`,
`replay/report.py`, or the refutation test file. The identity pin passing unmodified is
non-negotiable.