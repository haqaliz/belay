# Plan — aspect `parity-docs` (2026-09-16)

**Unit:** `feat/corpus-shell-routing/aliz` · **Spec:** `docs/planning/corpus-shell-routing/parity-docs/spec.md` · **PRD M8, S1.**

Strict TDD. Work in the worktree. Depends on `cli-wiring` being green (the flags must exist
before the guard can declare them). Run `uv run pytest tests/test_cli_flag_parity.py` after
every step, plus `tests/test_gate_docs.py` and the STATUS-pinning tests if any
(`tests/test_docs.py` if it exists — grep for `STATUS.md` first).

## Design (settled)

The guard's contract: `test_every_declared_flag_is_carried_by_exactly_the_declared_surfaces`
asserts `actual == EXPECTED` per declared flag; `test_every_flag_shared_by_two_replay_bearing_surfaces_is_declared`
is the discovery half. The moment the three corpus flags land on `corpus run` / `corpus add`,
and `corpus show` becomes replay-bearing, the table MUST declare the widened contract or the
guard fails red — that red test is this aspect's RED.

## Phase 1 — RED

- **Files:** `tests/test_cli_flag_parity.py`.
- **Steps:** after `cli-wiring` lands, run the parity file. It is RED because:
  1. `--shell-server` is now on 7 surfaces but declared for 4.
  2. `corpus show` carries `--shell-server` and `--corpus-dir` but is absent from
     `REPLAY_BEARING` and from those rows.
  Do NOT edit yet — record the red as the failing-test-first step.

## Phase 2 — GREEN

- **Files:** `tests/test_cli_flag_parity.py`.
- **Steps:**
  1. `REPLAY_BEARING` (`:42-53`): add `"corpus show"`. Rewrite the comment (`:37-41`): the
     recompute of `corpus show` re-invokes the server (it is in scope); `corpus
     label/list/score` still replay nothing and stay out.
  2. Row edits (each set AND its comment):
     - `--server` (`:62`): `_ALL - {"corpus run", "corpus show"}` — comment: each stored
       case carries its resolved server command, so neither batch nor display takes one.
     - `--replays` (`:68`): `_ALL - {"corpus run", "corpus show", "gate check"}`.
     - `--timeout` (`:75`): `_ALL - {"replay", "corpus run", "corpus show", "gate check"}`.
     - `--manifest-dir` (`:90`): `_ALL - {"phase0 run", "corpus run", "corpus show"}` —
       manifests are bundled IN the case dir.
     - `--shell-server` (`:85`): `{"verify", "phase0 run", "gate baseline", "gate check",
       "corpus add", "corpus run", "corpus show"}` — rewrite the comment: `corpus add`
       resolves the stored command for the target turn; `corpus run`/`corpus show`
       recompute whole traces whose second boundary is caller-supplied; the old
       "deliberately out of scope" rationale for `corpus add` is retired by this unit.
     - `--corpus-dir` (`:117`): `{"corpus add", "phase0 run", "gate check", "corpus show"}`
       (corpus show reads cases from it today).
  3. Confirm no other row needs `corpus show` (it carries only `--shell-server`,
     `--corpus-dir`, and its positional): `--no-claim-axis`, `--json`, `--turn`,
     `--invariants`, etc. stay as-is because `corpus show` does not carry them and their
     declared sets do not include it.
- **Validation:** `uv run pytest tests/test_cli_flag_parity.py` green.

## Phase 3 — docs

- **Files:** `docs/planning/corpus-trajectory-banking/prd.md`; `docs/STATUS.md`.
- **Steps:**
  1. `corpus-trajectory-banking/prd.md` out-of-scope line (`:145`): append a
     `**Closed 2026-09-16 (`corpus-shell-routing`):** ...` note — never edit the original
     text; mirror the AUDIT.md "Closed 2026-09-01" pattern.
  2. `docs/STATUS.md`: append a new entry (newest first, matching the existing format) —
     `corpus-shell-routing` ships: the three `--shell-server` flags, the named-cause SKIP
     decision, the parity widening. State the reclassification discipline: the no-flag
     mixed-trace recompute changes outcome (a real verdict → SKIP) on **constructed
     fixtures only** — no real banked two-server trajectory case exists (mint's 11 TPs
     never bankable, no-backfill), so **no real-world verdict moves** and the published
     numbers (`11/60 = 18.3%`, `precision 0.00`, `1/15`, `4/16`) stand unedited. Name what
     is NOT built (a `--server` override on `corpus run`, so the stored-shell shape stays
     SKIP — a named residual). Suite counts updated from the run.
  3. Grep for other "corpus run --shell-server" / "NOT-built" deferral mentions in docs and
     retire each with a correction note, never a silent edit.
- **Validation:** the STATUS/README pinning tests green (grep `tests/` for `STATUS.md`
  references and run them).

## Phase 4 — full check + commit

- Run the full suite subset that touches the corpus, CLI, and parity surfaces:
  `uv run pytest tests/test_cli_flag_parity.py tests/test_corpus_trajectory_run.py tests/test_corpus_run.py tests/test_phase0_cli.py`.
- **Commit message:**
  `test(parity): declare the corpus shell-axis surfaces; docs: retire the deferral`

## Edge cases & notes for the agent

- Every row edit's COMMENT must be updated with its frozenset — the guard is documentation
  as much as enforcement; a set that contradicts its comment is a review finding.
- Do not add `corpus show` to `--no-claim-axis` (declared out by decision; see spec).
- Historical `STATUS.md` entries are append-only — never edit them; the new entry
  supersedes by correction note.
- If `corpus show`'s parser order changes (flag added before its positionals), confirm the
  parity test's `_options` walker still resolves it (`_options` uses `cli._parser()`).