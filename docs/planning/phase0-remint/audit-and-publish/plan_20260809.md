# Implementation Plan — `phase0-remint/audit-and-publish`

**Aspect:** `docs/planning/phase0-remint/audit-and-publish/spec.md`
**PRD:** `docs/planning/phase0-remint/prd.md`
**Date:** 2026-08-09 · **Branch:** `feat/phase0-remint/aliz`
**Baseline:** `1626 passed, 1 skipped, 2 deselected` (v0.15.0)
**Precedent:** `docs/planning/phase0-mint-run/audit-and-publish/plan_20260809.md`, re-read for
the trajectory era: the audit now measures the trajectory rule's precision on real model
text (M6) in addition to the A1/A2 flag audit.

## 0. Prerequisites

| Prereq | Check |
|---|---|
| All completed stage ledgers committed | `docs/planning/phase0-remint/mint-run/ledgers/s5{a,b,c}.json` (s5c absent iff a stage gate stopped the run — the audit then covers what ran and the STOP is itself part of the decision section) |
| Flagged cases banked in the corpus | `uv run belay corpus list` shows the ingest (`corpus/local/`, gitignored — audit in place, commit only labels' metadata via the findings note) |
| `belay phase0 report` re-renders each ledger | spot-check before Phase 1 |
| Trajectory verdicts present in the ledgers | every instance's `trajectory` field carries `{"status", "cause", "evidence_count"}` (absent = rule not declared → STOP and reconcile) |

## 1. Decisions fixed before the audit

| # | Decision | Reasoning |
|---|---|---|
| **S-1** | The auditor is the owner; agents prepare evidence, never judgments | Pre-registration is a timing control, not an independence control (`PHASE0_RESULTS.md:63-65`) |
| **S-2** | Sampling rule (only if >30 flags): all control flags + all first-flag-in-instance + a random sample of the remainder, seed committed beside the sample; unaudited count stated | PRD req. 9, pre-registered |
| **S-3** | Root-cause keys are kebab-case and per-TP; independence is read off `(instance, tool)` pairs | The criteria's "independent" clause must be auditable by a stranger |
| **S-4** | The decision line is written by the owner AFTER the audit evidence is committed | The gate is the owner's call, on committed evidence |
| **S-5** | Every trajectory FAIL is adjudicated (no sampling of the trajectory axis): did the instance claim verification success, with zero (or failing) replayed commands, and did it have the suite-run ability and skip it? | M6 — the rule's first precision measurement; the `(instance, tool)` pair for a trajectory FAIL is `(instance, claim)`, counted under Rule C evidence |
| **S-6** | A trajectory PASS is NOT a clean bill — the rule's evidence is ANY replayed exit-0 command before the claim, not a suite run | The rule's documented approximation; noted per instance in the trajectory table, adjudication input |

## 2. Phases

### Phase 1 — Evidence inventory (agent-executed)

- **Goal:** every flag and every trajectory verdict collected, de-duplicated, and readable
  in one place.
- **Files:** `audit-and-publish/FLAGS.md` (NEW): per stage — flagged instances, flagged turns
  (index, tool, sub-verdicts, causes), corpus case ids, the pre-registered sampling outcome
  (full or sample + seed), and for each TP candidate the replay diff excerpt; plus the
  **trajectory table** — per instance: claim text (truncated per ledger honesty rules),
  classification, verdict, `evidence_count`, cause.
- **Validation:** FLAGS.md's case ids match `belay corpus list` output exactly; turn indices
  match the ledgers' `flagged_turns`; the trajectory table matches the ledgers' `trajectory`
  fields instance for instance.
- **Commit:** `audit: the flagged and trajectory inventory, from the committed ledgers`

### Phase 2 — The hand-audit (owner; main thread)

- **Goal:** every flag adjudicated; TPs labeled with root causes; the trajectory rule's
  precision measured.
- **Files:** `audit-and-publish/AUDIT.md` (NEW): per flag — TP / FP / unverifiable, the
  evidence read (trace + replay diff), and for TPs the `(instance, tool)` pair and root-cause
  key. The **trajectory adjudication section**: per FAIL — the claim, the zero-evidence
  finding, whether the suite-run ability existed (shell server available, task text invited
  verification), verdict TP/FP/unverifiable; per abstain — cause, plausibility; the precision
  table (judged/abstained by cause, FAILs adjudicated). `belay corpus label` applied to each
  audited case (TPs require `root_cause`).
- **Validation:** `uv run belay corpus score` prints precision/recall with a real
  denominator; every TP has a root cause; the FP rate is stated even if unflattering; the
  trajectory precision is stated or stated-unmeasured, never predicted.
- **Commit:** `audit: every flag and every trajectory verdict adjudicated, root causes recorded`

### Phase 3 — Hand-replay of one FAIL (owner, with an agent operating the commands)

- **Goal:** the symmetric FP guard's second half — the delta is real, not a rename/manifest
  artifact. Prefer a trajectory FAIL (Rule C's evidence requirement for any high rate).
- **Files:** `audit-and-publish/HAND_REPLAY.md` (NEW): the chosen FAIL, its trace, the replay
  command, the observed delta vs the recorded delta, and the verdict under the restored
  pre-state.
- **Validation:** the replayed verdict equals the ledger's; the delta is concrete (paths +
  content), not a wiring echo.
- **Commit:** `audit: one FAIL hand-replayed, its delta confirmed real`

### Phase 4 — Reproducibility check (agent-executed)

- **Goal:** a stranger can re-derive the number from the repo.
- **Files:** none (verification only) + `audit-and-publish/REPRODUCIBILITY.md` (NEW, short).
- **Procedure:** in a clean checkout/worktree of the branch, run `uv run belay phase0 report
  docs/planning/phase0-remint/mint-run/ledgers/s5{N}.json` for each committed ledger and
  assert the headline, FP rate, trajectory aggregate, and per-cause UNVERIFIED match the
  committed `acceptance-stageN.out` verbatim.
- **Validation:** byte-identical headline numbers; any mismatch is STOP — reconcile the
  artifact, never edit the output.
- **Commit:** `audit: the number is re-derivable from the committed ledgers`

### Phase 5 — The decision line (owner; main thread)

- **Goal:** PROCEED or PIVOT, written as plainly as the other, on the canonical criteria
  (`PHASE0_RESULTS.md:17-42`) with Rules B and C applied as applicable and the trajectory
  precision stated.
- **Files:** `docs/technical/PHASE0_RESULTS.md` (dated decision section; no published number
  re-derived except by the gate decision), `docs/technical/PHASE0_AUDIT.md` (new-audit
  correction block if the audit changes any prior figure — only via the dated-correction
  convention), `docs/ROADMAP.md` (R1 row + gate blocks, dated — R1's quantitative form is
  either tested or re-confirmed untested with the reason), `CLAUDE.md` (status block, dated),
  `docs/technical/CAPABILITY_ROADMAP.md` (C5 gate status block, dated).
- **Disclosure set (mandatory):** rate with denominator; FP rate stated; UNVERIFIED by named
  cause; exposure on BOTH lines (file-comparisons; trajectory judged/abstained by cause) with
  Rule B's mechanical reading applied; **the trajectory rule's measured precision on real
  model text** (M6); pool composition; coverage limits (filesystem-only, shell exclusion,
  NOT_COVERED, MCP boundary, the rule's "any command ≈ suite" approximation); the ToS
  assumption; "reproducible" in the decided words; the ordering disclosure; the D-1 gate
  supersession recorded.
- **Validation:** the decision line states PROCEED or PIVOT verbatim; every disclosure bullet
  present; the diff is dated.
- **Commit:** `results: the Phase-0 gate decision — PROCEED (or PIVOT), on the pre-registered criteria`

### Phase 6 — RUNBOOK walk (agent-executed, integrator reviews)

- **Goal:** the reproduce-the-number artifact works end-to-end and carries the trajectory
  era.
- **Files:** `docs/planning/phase0-corpus-run/RUNBOOK.md` (corrections committed).
- **Procedure:** follow the RUNBOOK from a clean state against the stage-2 artifacts; fix
  what the dig flagged (stale ledger example `:304-318`, stale case-id example `:348`) and
  add the trajectory-rule content (claim record, instance-level verdicts, trajectory FAIL
  ingest, the report's trajectory section); state in the commit which steps were walked.
- **Validation:** a fresh reader can reproduce `belay phase0 report` output from the RUNBOOK
  alone.
- **Commit:** `docs(runbook): walked end-to-end, trajectory-era corrections committed`

## 3. Edge cases

| Case | Handling |
|---|---|
| >30 flags | S-2 sampling; sample seed committed; unaudited count stated |
| A flag's replay is inconclusive | `unverifiable` label, stated; it neither helps nor hurts the TP count |
| A trajectory FAIL's replay is inconclusive | adjudicated `unverifiable`; excluded from the precision numerator with the denominator stated |
| Ledger/report mismatch in Phase 4 | STOP; reconcile the artifact — never edit the output |
| The audit contradicts a prior figure | Dated-correction block, never an edit of the published number |
| Near-zero with low exposure | Rule B: uninterpretable-about-agents reading written verbatim into the decision section |
| High trajectory FAIL rate | Rule C: controls + hand-replay evidence presented BEFORE the rate is published |
| A stage gate stopped the run (e.g. exposure gate re-fired) | The decision section records the STOP and its Rule A/B reading; the trajectory precision is stated-unmeasured for the missing stage |

## 4. Agent execution notes

- Agents own Phases 1, 4, and 6 (evidence inventory, reproducibility check, RUNBOOK walk).
  Phases 2, 3, and 5 are owner-main-thread: adjudication, hand-replay, and the decision are
  human acts on committed evidence.
- Phase 1's agent must not label any case — labels are human (the engine's own rule,
  mirrored in the pipeline: the agent prepares, the owner judges).
- The trajectory table is the unit's new load-bearing evidence (M6) — the inventory agent
  must transcribe it from the ledgers, never from memory of the runs.
- Checkpoints: after Phase 1 (inventory complete), after Phase 4 (reproducibility green),
  before Phase 5 (owner writes the decision on committed evidence).
