# HAND_REPLAY — One Flag Replayed End-to-End

> **STATUS: NOT WRITTEN.** Written by the OWNER after the AUDIT. Re-executes the
> verdict computation on one banked case via `belay corpus run` (trajectory cases
> have no per-turn diff, so replay re-executes the instance-level verdict).
> Verdict reproduced = MATCH stated; divergence = a finding.

## The case

- Case id / instance / turn:

## The replay

- Command: `belay corpus run` (targeted)
- Observed outcome:

## Verdict

- MATCH / divergence (named)
