# STAGE 3 FINDINGS — s6c

> **STATUS: NOT RUN — no result exists.** Updated after the run with what it showed
> (echoed facts and findings, exactly as a stage record should read). The verbatim
> run output lives in `acceptance-stage3.out`; the ledger in `ledgers/s6c.json`.

## Gates

- [ ] Denominator ≥50 **distinct fresh non-control** instances (controls partitioned out)
- [ ] Every UNVERIFIED to a named cause
- [ ] No `INSTRUMENT SUSPECT` (else STOP — wiring failure, never a result)
- [ ] Capture rate ≥50% (stop-loss: else publish the smaller denominator)

## Exposure lines (from `belay phase0 report`)

- File-comparisons:
- Trajectory judged/abstained by cause:

## Findings

(echoed, never asserted away)
