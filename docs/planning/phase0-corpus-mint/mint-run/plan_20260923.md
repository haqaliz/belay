# Implementation plan — `mint-run` run 2 (2026-09-23)

Source: `docs/planning/phase0-corpus-mint/mint-run/spec.md` (run-2 amendment) ·
PRD amendment. Grade: **MEASURED** with a deterministic freeze seam. Run-once under
the freeze protocol (Rule D: scripts committed first containing no result; each stage
run once; verbatim output committed next, whatever it says).

## Context (read first)

Run 1 (`acceptance-cm-stage1.sh` → `acceptance-cm-stage1.out`) STOPPED at its gate
(`NO_VERIFIABLE_TURNS: 2`, `INSTRUMENT SUSPECT`, UNVERIFIED 3/3). The instrument fix
(`effect-conformance-coverage`, v0.35.0) closed the annotation-abstention cause.
Run 2 reuses the committed registries verbatim, re-drives the four controls as a
declared owner decision, uses fresh roots `cm3`/`cm4` under `~/dev/at/holder/belay/`.
The `verify-parity` aspect must be merged before this run (the run itself uses the
frozen `belay phase0 run` form either way).

## Phase 1 — DETERMINISTIC: the run-2 freeze (agents)

**Goal:** the frozen invocation scripts, committed **containing no result** — the
freeze commit is the aspect's deterministic RED (the run-1 precedent: commit `70c190e`,
scripts only, grep-checked for result shapes).
**Files to create** in `docs/planning/phase0-corpus-mint/mint-run/` (model BOTH on
the run-1 scripts verbatim — read `acceptance-cm-stage1.sh`/`acceptance-cm-stage2.sh`
first; change only what the run-2 amendment changes):
- `acceptance-cm-run2-stage1.sh` — root `$HOLDER/mint/cm3`, registry
  `eval/instances/cm-stage1.json`, same prologue (`BELAY_EVAL_SERVER_ROOT`,
  `BELAY_CLAIM_AUTHOR` export, FS/SHELL server paths), same composition
  (`--provider claude-cli --model claude-opus-5 --max-steps 20 --request-timeout 120
  --toolset filesystem+shell`), verify via `belay phase0 run` with `--shell-server`
  BEFORE `--server`, ledger `$HOLDER/runs/cm-run2-stage1.json`, corpus-dir
  `$HOLDER/corpus-local`. Header states: declared second run, controls re-driven as a
  declared decision, roots cm3/cm4.
- `acceptance-cm-run2-stage2.sh` — root `$HOLDER/mint/cm4`, registry
  `eval/instances/cm-stage2.json`, ledger `$HOLDER/runs/cm-run2-stage2.json`, plus the
  run-1 stage-2 header's gate guard verbatim: "RUN THIS ONLY AFTER THE STAGE 1 GATE
  HAS BEEN EVALUATED AND CLEARED BY HAND".
**Freeze checks (grep, must all hold):** no `VERIFIED_`, `INSTRUMENT`, `NO_VERIFIABLE`,
`STOP`, `PASS`, `FAIL` result shapes; roots are absolute under `~/dev/at/holder/belay/`;
`--shell-server` precedes `--server` in both verify invocations; both registries
referenced by committed path; `BELAY_CLAIM_AUTHOR` exported in both.
**Validation:** `grep -nE 'VERIFIED|INSTRUMENT|NO_VERIFIABLE|STOPPED' <scripts>` empty;
the scripts' commands match the run-1 shape except roots/names/ledgers (diff by eye);
`uv run pytest -q` full suite green (nothing else changed).
**Commit:** `test(mint-run-run2): freeze the run-2 stage invocations — committed before the run, containing no result`
**⛔ CHECKPOINT — owner authorization for live spend is exercised at this commit** (the
PRD's sequencing consequence: "the frozen script will exist and be reviewable at that
moment"). Do NOT proceed to Phase 2 without explicit owner go.

## Phase 2 — MEASURED: stage 1, once (integrator + owner; agents never run this)

1. `bash docs/planning/phase0-corpus-mint/mint-run/acceptance-cm-run2-stage1.sh`
   (root cm3, 2 controls). Watch: quota breaker, capture rate.
2. Commit the verbatim output as `acceptance-cm-run2-stage1.out`:
   `test(mint-run-run2): stage 1 verbatim output — <whatever it says>`
3. **Stage-1 gate evaluation** (recorded explicitly, M2): a capture was produced; ≥1
   turn genuinely verifiable; both controls' dispositions reported, no FAILing control
   (D-3 — a FAIL VOIDS the run); no `INSTRUMENT SUSPECT`. Write `STAGE1_FINDINGS_RUN2.md`.
4. Branch per the pre-registered line (PRD amendment): gate clears → Phase 3;
   `INSTRUMENT SUSPECT`/`NO_VERIFIABLE_TURNS` → **STOP** — no stage-2 spend; record the
   finding as probe-scale inviability; the unit's remaining aspects run against what
   exists (corpus-banking/audit-and-publish report the honest zero).

## Phase 3 — MEASURED: stage 2, once (if the gate cleared)

1. `bash …/acceptance-cm-run2-stage2.sh` (root cm4, CTL-2 + CTL-3 + 8 fresh real,
   controls first). Stop-loss by stage.
2. Commit verbatim `acceptance-cm-run2-stage2.out`:
   `test(mint-run-run2): stage 2 verbatim output — <whatever it says>`
3. `STAGE2_FINDINGS_RUN2.md` — dispositions, exposure/trajectory lines per instance,
   A3 column state (M7: verdict or named cause, never `claim unrecorded`).

## Environment notes

- `HOLDER=~/dev/at/holder/belay`; servers/clones/corpus already exist there from
  run 1 — verify with `ls` before the run, never reinstall mid-run.
- Engine: this worktree's `uv run` (v0.37.0). State the version delta in the
  findings (run 1: 0.33.0; the NOT_COVERED boundary means UNVERIFIED rates across
  runs are NOT comparable).
- Local `claude` version stated (gate run used 2.1.228; measure the local one).

## Edge cases

- D-3 void → published as such (never hidden); pre-registered branch fires.
- Quota stop → `no_observation` re-arms; do not re-run captured/failed instances.
- Missing servers → `MissingServerError`, exit 2, preflight — nothing spent.
- A run that banks nothing is a recorded result, not a red failure.

## Agent notes

- Agents build Phase 1 only. Phases 2–3 are the integrator's + owner's (live spend,
  freeze protocol — the house rule: live runs never delegated).