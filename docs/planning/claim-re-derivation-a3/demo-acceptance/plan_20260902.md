# Plan — demo-acceptance (acceptance 4, re-scoped) · 2026-09-02

> Aspect spec: `../demo-acceptance/spec.md` · PRD: `../prd.md` · Decision D1 confirmed 2026-09-02.
> Depends on the `evaluator` aspect (fake author seam). The synthetic fixture produced here is
> the shared input to the `corpus` and `surfaces` aspects.
> TDD: RED → GREEN → REFACTOR.

## 1. Setup

- New test files only (this aspect ships tests + docs, no new engine code):
  - `tests/test_a3_corrupt_success_fixture.py` — the synthetic corrupt-success shape.
  - additions to `tests/test_demo_capture.py` — the demo-stays-green-with-A3 pin.
- New fixture: `tests/fixtures/claim_liar_server.py` — a fake MCP server (stdlib subprocess
  server, pattern of `tests/fixtures/weakening_editor_server.py` / `shell_command_server.py`)
  offering exactly two tools: `run_process` (whitelisted argv `python run_tests.py`) and
  `write_file`; every tool declares `openWorldHint: false`.
- Reused rig: `tests/conftest.py` `run_traced`/`trace_of` (real TraceWriter capture),
  `CLIENT_LINES` scripted client, `run_over_pipes`.

## 2. The synthetic corrupt-success fixture (the "liar" shape)

Capture, in-test (deterministic, no network, fast — no 300s replays):
1. Client drives: `write_file("run_tests.py", "<suite that exits 1>")` — the agent "edits source
   and never runs a command"; the workspace's suite **fails** at the final state.
2. Driver appends a claim record: `"All tests pass."` (VERIFICATION-classified by
   `classify_claim_text` — pinned by the existing classifier tests).
3. Trace has: `tools/list` incl. `run_process` (command tool **offered**), zero `run_process`
   turns, final turn = `write_file`.

Expected verdicts on this fixture (each asserted):
- **A1 trajectory** (`evaluate_trajectory_invariant`): **FAIL** — VERIFICATION claim, command
  tool offered, zero replayed exit-0 command evidence (`trajectory.py:486-489` shape).
- **A3** (`evaluate_claim` with a fake author whose check runs the suite —
  `argv=("python3", "run_tests.py")`): **FAIL**, exit 1, check source + exit code surfaced.
- **A2 per-turn** (`verify_turn`): PASS / UNVERIFIED — **never FAIL** (the axes are independent;
  asserted, not assumed — `demo-acceptance/spec.md` acceptance 2).
- `--no-claim-axis`-equivalent: an A3-absent run of the same fixture leaves A1/A2 identical
  (the refutation's seed — surfaces runs it for real).

The demo-stays-green pin (committed capture, `demo/capture/trace-20260827T001428Z-e23f999d.jsonl`):
- Fake author → check `("python3", "run_tests.py")` (exists in the demo workspace — the server's
  own whitelisted argv shape). Final state = fixed app.py → exit 0 → **silence**: no A3 verdict.
- Assert the existing pins unchanged: 7/7 PASS, trajectory PASS, 0 UNVERIFIED — with A3 present
  (`tests/test_demo_capture.py:433-477`).
- Session-scoped fixture (reuse the `report` fixture pattern) — the extra final-turn replay
  (~44 s) happens once per session.

## 3. Implementation phases

**Phase 1 — RED: the liar fixture.** `tests/test_a3_corrupt_success_fixture.py`: build the
capture, assert A1 trajectory FAIL, A3 FAIL (exit 1 + source surfaced), A2 never FAIL on the
same turns. The A3 call uses the stub replay seam (fast) for the final-state materialization
— no real replay needed for the RED; the real path is Phase 3.
- Commit: `test(claims): RED — the corrupt-success fixture FAILs on A1 and A3, never on A2`

**Phase 2 — GREEN.** The fixture is test-side; GREEN means the assertions hold against the
existing engine + the `evaluator` aspect's shipped `evaluate_claim` (fake author). If a
fixture bug surfaces (server contract, correlation), fix the fixture — never the engine.
- Validation: `uv run pytest tests/test_a3_corrupt_success_fixture.py -q` GREEN.
- Commit: `test(claims): the corrupt-success fixture — A3 FAIL corroborates A1 FAIL`

**Phase 3 — real final-state materialization.** Swap the stub replay for the real path in the
fixture test: `evaluate_claim` replays the final `write_file` turn against the real
server_command (fast — write_file replays in ms) and runs the check in the materialized
workspace. Asserts the exit-1 FAIL comes from the REAL restored final state.
- Commit: `test(claims): the check runs against the materialized final state`

**Phase 4 — the demo pin.** Additions to `tests/test_demo_capture.py`: fake author → suite
check → silence; all existing pins hold with A3 present. darwin-gated like the existing capture
tests (`test_demo_capture.py:284-290`); linux side measured in-container by the docker job.
- Validation: `uv run pytest tests/test_demo_capture.py -q` GREEN (darwin).
- Commit: `test(claims): the launch demo stays green with A3 present`

**Phase 5 — docs.** `demo/README.md` + `PROVENANCE.md` + launch-demo planning + the C8 section
of `docs/technical/CAPABILITY_ROADMAP.md` record the D1 re-scope with the 2026-09-02 date;
`tests/test_demo_assets.py` guard stays green.
- Commit: `docs(claims): acceptance 4 re-scoped — demo is the negative control, fixture carries the FAIL`

## 4. Edge cases & error handling

- `write_file` turn replay must be deterministic (no clock, no network) — use the real
  sandbox where the platform allows, else the stub seam (darwin-gated, mirroring
  `test_demo_capture.py:284-290`).
- The claim record must be appended AFTER the last turn (seq ordering, `trace.py:389` rules) —
  the fixture asserts seq monotonicity.
- If `run_tests.py` conflicts with the demo server's whitelist — the liar fixture uses its own
  server, not `demo/server.py`; no conflict.

## 5. Determinism

- Capture built in-test with scripted bytes — byte-reproducible; no network; no sleeps.

## 6. Agent execution notes

- This aspect is the shared input to `corpus` (bank the liar case) and `surfaces` (the
  refutation). Keep the fixture's trace in a reusable helper
  (`tests/fixtures/claim_liar_capture.py`-style builder or a fixture function in conftest) so
  later aspects reuse it instead of rebuilding.
- Checkpoint: after Phase 4, the integrator confirms the demo pin timing is acceptable
  (one extra ~44 s replay per session).