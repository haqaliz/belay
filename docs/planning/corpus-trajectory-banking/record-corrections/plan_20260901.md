# Plan — corpus-trajectory-banking / record-corrections (2026-09-01)

**Source:** `docs/planning/corpus-trajectory-banking/prd.md` (S2, goal 4's no-backfill
statement) + `docs/planning/corpus-trajectory-banking/record-corrections/spec.md`.
**Executor:** one agent. **Branch:** `feat/corpus-trajectory-banking/aliz` (worktree
`.claude/worktrees/feat-corpus-trajectory-banking`).
**Prerequisites:** aspects 1 and 2 landed on the branch (the record states shipped facts).

## 1. Setup checklist

- Docs-only + one version bump. Read `RELEASING.md` before the release phase.
- Check `CHANGELOG.md`'s current head (the `[Unreleased]` section and the most recent
  dated block) and mirror its exact shape.
- Machine-checked docs: confirm whether any test scans these files
  (e.g. `tests/test_docs*.py`, `test_platform_gate_named_causes.py` covers other files) —
  run the full suite before and after.

## 2. Implementation phases

### Phase 1 — The record

- **Goal:** every committed doc that described the old namespace or the open follow-up is
  corrected or closed, with the no-backfill fact stated.
- **Files to modify:**
  1. `docs/planning/trajectory-success-invariant/corpus-trajectory/spec.md` — ingestion
     section: trajectory cases mint `f"{source_trace_id}-trajectory"` (instance-level
     namespace, disjoint from per-turn `-turnN`; turn indices are integers, so no
     collision), the collision rationale, and note that plan edge-case row 134 ("both
     cases ingest") now holds for the final-turn shape.
  2. `docs/planning/mint-shell-toolset-run/audit-and-publish/AUDIT.md` — the
     corpus-banking finding's follow-up line records the closure (fixed in v0.26.0 by
     this unit). The historical finding stands verbatim: zero of 23 banked, because the
     s6 captures are gone — **nothing is backfilled**.
  3. `docs/STATUS.md` — append entry, newest first per the file's convention: the
     namespace split; both shapes coexisting and recomputing MATCH; the
     unrestorable-pre-state stay-unbankable contract; `corpus score` still `n/a` until a
     future mint runs under the fixed ingest (no backfill); the reclassification
     discipline (`11/60 = 18.3%`, `precision 0.00`, `1/15`, `4/16` stand unedited).
  4. `docs/technical/PHASE0_RESULTS.md` — ONLY if the file's own correction discipline
     permits: the corpus-banking finding's "recorded follow-up defect" line may gain a
     pointer to the closure. Read the file's correction-policy section first; if the
     policy says historical findings stand unedited, add nothing and say so in the commit
     message. **Never re-derive or restate a number.**
- **Validation:** full suite green; `grep -rn "turn<final>" docs/planning/trajectory-success-invariant`
  finds no stale namespace prose.
- **Commit:** `docs(corpus): the trajectory case namespace is disjoint; the AUDIT follow-up is closed`

### Phase 2 — The release commit

- **Goal:** ship the unit as a release, per `RELEASING.md`.
- **Files:** `pyproject.toml` (`version = "0.25.0"` → `"0.26.0"`), `CHANGELOG.md`
  (move `[Unreleased]` notes into a dated `0.26.0` section — write the entry describing
  this unit: trajectory FAILs bank as corpus cases in a disjoint namespace; include the
  honest line that no historical case was backfilled).
- **Validation:** `uv run pytest` full suite green; `uv run belay --version` reports
  0.26.0.
- **Commit:** `release: v0.26.0 — trajectory FAILs bank as corpus cases`
- **NOT in this branch:** tagging and pushing. Per `RELEASING.md` the tag is pushed after
  merge to `master` with CI green (macOS + Linux + docker jobs) — that is the owner's
  step, performed after the PR merges.

## 3. File-by-file build order

1. The four doc files (Phase 1) → 2. `pyproject.toml` + `CHANGELOG.md` (Phase 2).

## 4. Testing strategy

- Full suite after Phase 1 and after Phase 2 (release bumps have historically broken
  version-dependent tests — check `tests/test_cli.py`-style version assertions).
- `uv run belay --version` after Phase 2.

## 5. Environment & determinism

- None new. The version bump must match the tag that will be pushed later
  (`RELEASING.md`: "The tag must match the version in pyproject.toml").

## 6. Edge cases & error handling

- **Stale prose elsewhere:** `grep -rn "-turn1\"\|-turn2\"\|-turn<final>\|turn{final}" docs/`
  and `CLAUDE.md` for other spots naming the old trajectory id — the worktree CLAUDE.md is
  the committed one; if it names the old namespace, correct it in Phase 1 (it did not, at
  handoff time).
- **PHASE0_RESULTS policy:** the file is governed by its own correction discipline — read
  it first; a historical record that must stand unedited stays unedited.
- **CHANGELOG format:** mirror the most recent dated block exactly (heading style, "What
  this does NOT do" paragraph, version links if present).

## 7. Agent execution notes

- Do NOT touch `docs/ROADMAP.md`, `docs/technical/CAPABILITY_ROADMAP.md`, or any published
  number (`11/60`, `precision 0.00`, `1/15`, `4/16`, `recall n/a`).
- The no-backfill sentence must appear in STATUS.md AND CHANGELOG.md — the docs state
  plainly that the value is forward-looking.
- Checkpoint for the integrator: after Phase 2, the branch is ready for PR + the owner's
  tag step.