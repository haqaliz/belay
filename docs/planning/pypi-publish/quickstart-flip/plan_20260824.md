# Plan: quickstart-flip — 2026-08-24

Feature: `pypi-publish` (L4) · Aspect: `quickstart-flip` · Source:
`docs/planning/pypi-publish/spec.md` + `prd.md` M3/M4/M6/S1/S2.
Executed by the agents team under strict TDD in Phase 6 (RED → GREEN → REFACTOR).

## 1. Project setup checklist

- No new dependencies, no config changes. Doc edits + one new test module in the
  default suite (fast, network-free).
- Nothing to install: the worktree already has `uv sync` done.

## 2. Implementation phases

### Phase 1: RED — the docs-consistency test fails against today's README

- **Goal:** `tests/test_quickstart_docs.py` written; it FAILS because the current
  README still carries the caveat (provable now — the "until then, run from source"
  string is present at `README.md:56`).
- **Prerequisites:** none.
- **Files to create:** `tests/test_quickstart_docs.py` —
  - helpers: `_repo_root()` (parents[1]), `_read(rel)` returning file text;
  - `test_readme_has_no_run_from_source_install_caveat` — asserts neither
    `"until then, run from source"` nor `"once v0.1.0 is published"` appears in
    README.md;
  - `test_readme_install_commands_name_the_real_distribution` — asserts
    `belay-harness` appears in the install block region (the lines around the
    `uv tool install` command) and `"the command is `belay`"` claim still holds
    (assert `belay` appears in the same region);
  - `test_readme_and_releasing_agree_on_the_distribution_name` — both README.md and
    RELEASING.md contain `belay-harness`;
  - `test_pyproject_distribution_name_matches_readme_claims` — pyproject `name` is
    `belay-harness` (regex like `_project_version`) and `[project.scripts] belay`
    exists, matching the README's claims;
  - `test_stranger_timing_runbook_install_command_matches_readme` — if
    `docs/planning/pypi-publish/stranger-timing/runbook.md` exists, its install
    command equals the README headline install command; otherwise `skip` with a
    named cause (`runbook-not-yet-written` — the aspect is sequenced after this
    one; the skip is a placeholder that becomes live in A3).
- **Validation:** `uv run pytest tests/test_quickstart_docs.py -q` → the caveat test
  FAILS (RED proven); the rest pass or fail per current state.
- **Commit message:** `test(docs): RED — the quickstart must be the live install path`

### Phase 2: GREEN — the doc edits

- **Goal:** README / RELEASING / CHECKLIST edited so the consistency tests pass.
- **Prerequisites:** Phase 1 RED committed.
- **Files to modify:**
  - `README.md:54-62` — rewrite the install block:
    - requirements line kept (macOS/Linux, Python 3.10+, uv recommended);
    - headline: `uv tool install belay-harness` (+ `# or: pipx install belay-harness / pip install belay-harness`);
    - kept verbatim-in-spirit: "the distribution is `belay-harness` (the name `belay`
      is taken on PyPI); the command is `belay`";
    - add the S2 live note (version-agnostic, e.g. "published to PyPI — the version
      the README documents is the one `belay --version`-adjacent facts describe";
      keep it factual and rot-proof, no hardcoded version);
    - `belay --help` line kept;
    - Docker quickstart (`README.md:64-84`) untouched; Develop section
      (`README.md:287-297`) untouched.
  - `RELEASING.md:22` — replace the stale "C7 → v0.2.0" example with a real pair
    (e.g. "the docker-selfhost slice → v0.21.0") or neutral phrasing ("one 0.x.0 bump
    per shipped capability/milestone").
  - `docs/planning/launch-readiness/CHECKLIST.md:193-199` — L4 rewritten:
    - corrected fact: channel live since 0.1.0 (2026-07-18), current 0.21.1;
    - completion contract: "work shipped — CI-proven artifact install, quickstart
      flipped; the <15-min stranger timing is the remaining clause, per
      `docs/planning/pypi-publish/stranger-timing/runbook.md`";
    - ✅ box stays unchecked;
    - Progress log (`:266-271`): append a row — `2026-08-24 | pypi-publish | L4 |
      work shipped (artifact-install CI, quickstart flip); metric pending per the runbook`.
- **Validation:** `uv run pytest tests/test_quickstart_docs.py -q` green;
  `uv run pytest -q` full suite green; `grep -rn "until then, run from source"
  README.md` empty.
- **Commit message:** `docs(quickstart): the install path is the live PyPI channel (L4 flip)`

## 3. File-by-file build order

| File | Purpose | Key exports | Depends on |
|------|---------|-------------|------------|
| `tests/test_quickstart_docs.py` | machine-checked docs truth | 5 tests + 1 conditional | — |
| `README.md` | install block flip | — | RED test |
| `RELEASING.md` | stale example fix | — | — |
| `docs/planning/launch-readiness/CHECKLIST.md` | L4 wording + progress log | — | — |

## 4. Testing strategy

- The consistency test module is the acceptance (PRD M4). Unit-style string/parse
  assertions on committed docs — deterministic, no network.
- Full suite green after each phase (`uv run pytest -q`).

## 5. Environment & determinism

- None beyond the suite: no env vars, no network, no clock.

## 6. Edge cases & error handling

- **Docs drift later (a future PR re-adds a caveat or renames the dist):** the
  consistency test fails in the default suite — that is the point (machine-checked
  claims are the repo's convention).
- **The runbook does not exist when this aspect lands first:** the runbook-consistency
  test skips with the named cause `runbook-not-yet-written`; A3 removes the skip by
  creating the runbook. A named-cause skip is the repo's accepted intermediate state,
  never a silent pass.

## 7. Agent execution notes

- Checkpoint after Phase 1 (RED committed) before the doc edits — the README diff
  deserves a human look even on the fast track.
- Preserve the README's honest coverage statements when rewriting the install block —
  do not delete or weaken the coverage line while touching the section.
- The CHECKLIST edit must keep L4's ✅ box unchecked: marking DONE is the operator's
  act after the timed run (R10), not the PR's.